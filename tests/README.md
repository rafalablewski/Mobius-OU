# /tests/ — drafts, sketches and design R&D

Scratchpad for things that **aren't** part of the deployed site. Anything in
this folder is for review, exploration or comparison and should not be linked
from the production app.

## Convention

- All draft sketches, wireframes and design R&D go under
  `/tests/sketches/<name>.html` (or `.svg`).
- Standalone exploratory pages (logo concepts, palette tests, type specimens,
  competitor comparisons) live at the top of `/tests/`.
- Nothing in `/tests/` ships in the Vite build — it sits outside `public/` and
  outside `src/` so it never reaches `dist/`.
- If a sketch becomes a real component, port it into `src/components/` and
  delete the draft.

## Current contents

| Path | What |
| --- | --- |
| `tests/logo-concepts.html` | Standalone preview of logo wordmark concepts (was `public/assets/logo-concepts.html`). |
| `tests/sketches/program-page-improvements.html` | Five proposed improvements to `ProgramPage.jsx` benchmarked against Astons' Vanuatu landing — ideas + hand-drawn-style wireframes. Includes a second section with five UX variations of idea 01's "Brief a Partner" panel (inline strip, email-gated dossier, expanding pill, dock-on-scroll, calendar-first). |
| `tests/sketches/freshness-badge-redesign.html` | Fifteen redesigns of the program-page "UPDATED 2026.Q1" badge (`Breadcrumb.jsx` `freshness` prop), in three rounds. Round one (pragmatic): dossier stamp, masthead line, changelog tab, gutter mark, status pill + tooltip. Round two (bolder, decorative): wax-seal monogram, watermark chapter mark, postage stamp, redline correction, almanac · roman numeral. Round three (institutional, restrained — audit-report register): document-control block, ISO datestamp, filing reference table, semver tag, standfirst compliance line. Each shown as a pencil sketch on dark hero ground plus a live HTML/CSS micro-mock. |
| `tests/sketches/calendar-redesign-concepts.html` | Ten redesigns of the program-page booking card (`BriefPanel.jsx` — "Pick a 30-minute slot"), drawing on Apple, Tesla, Lucid Motors and Adobe. Apple — Stocks-style frosted card with weekday chips; Apple Wallet perforated boarding-pass stub. Tesla — touchscreen slab with oversized 88px time and single CTA; Tesla service-appointment list with "soonest" tag and host monogram. Lucid — horizontal hairline cluster with three pips; Lucid Air interior with champagne monogram and three glass blades. Adobe — Creative Cloud panel with per-row swatches and ⌥1/⌥2/⌥3 keyboard shortcuts; Adobe XD split dock pairing month grid + day's slot list. Hybrids — Apple × Lucid clock-face dial picker; Tesla × Adobe split panel that exposes booked-vs-open scarcity. Each is a live HTML/CSS render on dark hero ground, with TOC, baseline of today's card, and a per-idea "vs. today" contrast note. |
| `tests/sketches/calendar-editorial-fit-ideas.html` | Ten visual variants for the shipped Tesla `BriefPanel` whose only goal is to make it fit the editorial page register without changing structure or behaviour — the panel currently reads as software dropped onto a dossier. Same data + same logic in every panel; only typography, colour, ornament and surface change. Each rendered desktop + mobile side-by-side; one shared timer drives every live countdown. (01) Cream card invert — full palette flip to cream BG / dark text / italic Fraunces bigtime / dark CTA slab — the boldest swap of the ten; (02) Doc-control register — booking panel becomes another doc-control row in the page's audit format (HOST · STATUS · OPEN · CLOSES tabular cells, hairline-bordered slot grid, ghost-outline CTA), reusing the existing `.ht-breadcrumb__doc-control` pattern verbatim; (03) Letterpress almanac — full editorial serif on a parchment cream ground, Cormorant italic 300 at 88 px for the bigtime, CTA becomes a serif italic line bracketed by em-dashes (no button); (04) Pitch-card frame — reuses the page's `.ht-pitch-card` surface verbatim (1 px gold border at 30 %, white-on-black gradient, top-left + bottom-right corner ticks), Fraunces italic numerals, hairline gold ghost CTA; (05) Magazine sidecar — print-magazine sidebar with thick masthead rule, italic byline, drop-cap-style bigtime, dotted-rule slot list; (06) Watermark monogram — 280 px Fraunces italic 'M' behind the content at 6 % opacity, otherwise dark slab with serif italic numerals; (07) Wax-seal accent — 56 px circular gold seal at the top-right corner with dashed inner ring and italic 'M' rotated 8°, otherwise restrained dark slab — reuses the freshness-badge round-two seal direction; (08) Calling card centred — formal cream calling-card layout with double gold rules near top + bottom edges, 96 px Cormorant italic centrepiece, dotted-rule slot list, hairline-bordered Reserve pill; (09) Linen letterhead — laid-paper texture (CSS-only cross-weave) on warm cream, letterhead masthead with Cormorant italic wordmark + monospace reference tag, gold-outline CTA; (10) Tesla-restrained editorial — most conservative: keeps the shipped layout exactly, only shifts Manrope-200 → Fraunces italic 300 for the numerals, white border → low-opacity gold, solid white CTA → cream-outline pill. |
| `tests/sketches/program-navigation-concepts.html` | **Round 2.** Round one parked on Idea I — the Roman index card (front-matter "Contents" page-zero with I–IX entries before the nine 100vh chapters). This file keeps the parent (I.0) and pushes it five ways, each pulling on a different register without abandoning the contents-page idea. Behavioural contract is constant across all five: hero ends → contents page (page 0) → click any chapter → scroll-snap into the corresponding 100vh panel → slim chapter band ("II · INVESTMENT") at the top of every chapter view. What changes is the surface of the contents page. Pencil sketch on cream + live HTML/CSS micro-mock on dark hero ground per derivation + behaviour register (surface · layout · active · mobile) + "vs the parent (I.0)" contrast. (I.0) Roman index card — the keeper, baseline cream contents card with Roman numerals; (I.A) editorial masthead — newspaper front-matter with ink masthead block (MOBIUS DOSSIER · No. PRT-GV · 2026.Q1), gold hairline rule, two-column layout — chapter list with dotted leaders + tabular page numbers in the wider column, standfirst/editorial brief in the narrower, drop-cap-led; (I.B) 3×3 card-deck — modern product grid of nine cards on cream, each card carrying a Roman numeral, chapter title, one-line dek and monospace doc-ref + arrow at the foot, hover lifts -4 px and ramps the hairline to gold; (I.C) type-spec stairway — pure typography, no card frame, nine Cormorant italic 22 px chapter titles stacked vertically with tabular Roman numerals and small-caps glosses, hairline rules between rows, magazine "in this issue" register; (I.D) audit-report contents — front-matter as filing reference, five monospace columns (SECTION · CHAPTER · DOC-REF · REVISED · STATUS) with green-dot CURRENT/ON-PAGE indicator and a redline DRAFT row exposed honestly, reuses the existing `.ht-breadcrumb__doc-control` register language; (I.E) cinema marquee black-card — polarity flip, ink ground, three gold pips up top, "MOBIUS DOSSIER PRESENTS · PORTUGAL · GOLDEN VISA" in tightly-tracked Manrope caps, italic Cormorant chapter list centred in gold, "AN AUDIT BY MOBIUS & PARTNERS" credit line and a letterspaced BEGIN ↓ directive at the foot, opera-programme / cinema title plate. |
| `tests/sketches/calendar-fomo-countdown-ideas.html` | Fifteen polish variants for the shipped Tesla `BriefPanel`. Each combines: a FOMO scarcity signal ("2 left"), a live countdown chip, a specific mobile reflow, plus three constants — host **Rafał A.**, an **AVAILABLE** indicator with a green flashing dot in the top-right, and the classic **NDA-FIRST · NO MARKETING · PRIVATE CALL** footer microcopy alongside a serif "Pick another time →" alt link. Desktop + mobile rendered side-by-side per idea on dark hero ground; one shared JS timer drives every live counter. **Reworked v2 (01–10):** (01) gold "2 LEFT" pill in slim countdown bar; (02) editorial serif italic "Two slots left — Rafał's week closes in 2 h 14 m"; (03) 12-pip warm capacity rail + flip-clock HH/MM/SS + scroll-snap mobile; (04) feed strip carrying both social proof + "2 left" + warm foot timer + bottom-sheet mobile; (05) per-slab tags (LAST / 2 LEFT / OPEN) + denser 3-up; (06) terracotta stopwatch row "Closes today · 2 left · 02:14:38" + sticky-CTA mobile; (07) 35° corner ribbon "ALMOST FULL · 2 LEFT" + inline meta-line timer + drawer-up animation; (08) breathing chip "2 LEFT · closes HH:MM:SS"; (09) split-status pair row + per-slab chips + collapsed mini-bar mobile; (10) animated gold hairline rule "2 LEFT this week · 02:14:38". **New v2 (11–15):** (11) marquee status ticker scrolling AVAILABLE · 2 LEFT · CLOSES IN 02:14:38 · LAST RESERVED 4 MIN AGO with edge-mask gradients (22 s desktop / 28 s mobile); (12) circular SVG progress arc that depletes around the live HH:MM, with "2 LEFT" pill alongside; (13) audit-report doc-control register — monospace tabular row STATUS · OPEN · CLOSES with green-dot AVAILABLE inline (matches the program-page doc-control); (14) twin-counter pairing "Next slot in 18 h 23 m" (neutral) with "Window closes in 02:14:38 · 2 LEFT" (warm), cells stack on mobile; (15) reserved-stamp slabs — whole-week 5-up grid with redlined "BOOKED" stamps on three days so the two open slots pop visually, countdown in the legend row. |
| `tests/sketches/contents-expanded-redesign.html` | **Three finalists** for the *expanded* state of the program-page contents disclosure (`ProgramSpine.jsx` → `ContentsPage()` → `.ht-spine__contents-list`). Today's open state stacks nine ~95 px rows with dotted leaders and decorative page numbers — ~850 px of print-appendix dropped onto the page. The toggle is held constant; only the surface of the open state changes. (I) Letterpress mosaic — 5×2 hairline grid of nine chapter cells plus a tenth colophon cell carrying edition + imprint, with pitch-card corner ticks (~250 px); (II) Phased trio — three columns grouped Orientation (I–III) / Substance (IV–VI) / Evidence (VII–IX) so the contents page tells the reader what the dossier is for (~220 px); (III) Stepper journey bar — nine 34 px nodes on a hairline track, italic Cormorant numerals inside each node, current chapter ink-filled with a gold dot, single horizontal row that scrolls on mobile rather than collapsing (~150 px). Each finalist: rationale + "vs today" contrast + behaviour register (Surface · Active · Mobile) + live HTML/CSS micro-mock. Pruned from a wider ten-draft round preserved in the commit history of branch `claude/redesign-expandable-content-KR2tB` (compact ledger, audit doc-control register, editorial running text, drop-cap magazine TOC, marginalia rail, running heads with deks, pure typography index). Supersedes the earlier abandoned `contents-disclosure-redesign.html` (which stopped at 3 of 10 promised designs). |
| `tests/sketches/malta-valletta-skyline.svg` + `.html` | Hand-drawn pen sketch of Valletta from Sliema — the iconic St Paul's Pro-Cathedral spire and Carmelite Church dome rising over a dense honey-stone building cluster, with a flock of birds, sea hatching and a small boat. Synthesised from three references: composition from the schematic line-drawing reference (ref 1), pen weight + dome hatching from a sepia-ink sketch (ref 2), looseness + bird flock + italic-serif "Malta" signature from a coloured travel sketch (ref 3). Wobble comes from an SVG `feTurbulence` + `feDisplacementMap` filter applied at two intensities (heavy on primary masses, fine on windows and hatching) so the pen feels hand-pressed without losing legibility at small sizes. Stroked in `currentColor` so the host page tints it (gold-on-ink for the dossier hero, ink-on-cream for body sections). The companion `.html` previews the SVG on cream / bone / ink grounds plus a dossier-hero mock with the skyline as a faint gold ornament beneath the title. Vector — no LCP cost. |
| `tests/sketches/section-pager-button-ideas.html` | Ten drafts of a chapter-by-chapter pager button for `ProgramSpine` — companion / replacement to today's lone gold back-to-top disc. Behavioural contract held constant across all ten: click → smooth-scroll to the next chapter `id` (or previous), with `scroll-margin-top: var(--ht-spine-band-top)` so it lands flush under the page header and chapter band; keyboard mirror **J**/**K** or **↑**/**↓**; disabled at I and IX. Per idea: pencil sketch on cream + live HTML/CSS mock on dark hero ground + behaviour register (surface · active · mobile) + "vs today" contrast. (I) Stacked dock — two 48 px gold discs ↑/↓ with a "II / IX" monospace counter beneath; (II) Right-edge dot rail — vertical hairline + nine pips, active grows and rings, lone ↓ disc near foot for the simple gesture; (III) Bottom-centre prev/next pill — frosted ink capsule with three labelled cells "← I · About | II · Investment | Benefits · III →"; (IV) Keyboard-hint disc — ↓ disc plus tiny `J`/`K` chips for vim-literate readers; (V) Progress dial — single 60 px disc with filled gold arc that fills as you advance, italic Roman numeral centre, click forward / long-press back; (VI) Edge tab — gold paper-bookmark tab pulled from the right margin, ↑ on top half, ↓ on bottom; (VII) Inline chapter-foot ribbon — full-width sticky-bottom ribbon with the next chapter's italic title and chevron, no floating widget; (VIII) Spine band counter as button — bolts a tiny ↑/↓ pager into the existing sticky chapter band, zero new floating chrome; (IX) Hairline film-strip — 1 px gold rule along the bottom edge with nine pips, active pip pops a Cormorant-italic title; (X) Letterpress numeral chip — cream card with big italic Roman + dotted hairline + small-caps "NEXT" line, dossier register. |
| `tests/sketches/press-line-line-cleanup.html` | Six redesigns of the home/about "AS SEEN IN" strip (`PressLine.jsx` marquee variant + `.ht-stats-area` "14 / 100 / 32" tile) that fix the doubled-hairlines problem: today the press-line carries top + bottom rules in `rgba(11,15,20,0.20)` and the stats area carries top + bottom rules in `--ht-border-color` (`#E2DBCE`) — two different colour temperatures, four parallel rules stacked over ~80 px. Each idea: pencil ASCII sketch + live HTML/CSS mock + behaviour register (Surface · Rules · Change · Mobile) + "vs today" contrast + minimal-diff CSS/JSX snippet. (0) Baseline today; (I) No press-line border — strip both rules off `.ht-press-line`, let stats slab frame the section, four rules drop to two — *recommended ship*; (II) Single shared rail — wrap press + stats in one bordered slab with a mid-rule divider, three rules total; (III) Unified hairline colour — most conservative, swap `.ht-press-line`'s ink-tinted rules for `--ht-border-color` so all four rules match, zero JSX change — *recommended fallback*; (IV) Label-led running head — "AS SEEN IN" gets a trailing hairline ornament (program-page register), press-line bottom rule doubles as stats top rule, shared edge; (V) Press + stats fused — one ruled credibility slab, gold pip as the only internal divider, two outer rules only; (VI) Centred masthead, no rules — press strip drops to a centred typographic line on cream, boldest swap, only the stats slab keeps rules. |
| `tests/sketches/footer-social-proof-swap.html` | Five drafts that swap the footer's `<PressLine variant="static" />` block at `Footer.jsx:66` (a duplicate of the homepage press credit) for the real Google + Trustpilot rating badges already produced as `/public/google.svg` and `/public/trustpilot.svg` and live on the Contact page via `RatingProof.jsx`. Each rendered on the actual footer ink ground next to a faithful copy of the existing copyright/legal row, with behaviour register + "vs today" contrast + one-line `Footer.jsx` swap snippet. (0) Baseline today — static PressLine on ink; (I) Native badges side-by-side — direct port of `RatingProof.jsx` at 96 px (vs Contact's 200 px), retains the green Google top-rule and Trustpilot card chrome, loudest signal; (II) Eyebrow + left-aligned row — small-caps "INDEPENDENT RATINGS" header over 84 px badges, matches the existing footer widget-column register — *recommended ship*; (III) Single hairline rail — one ivory top rule, no bottom rule, 88 px badges float on ink, fewest lines; (IV) Compact ink-framed cards — 64 px badges each inside a thin ivory hairline frame, smallest footer footprint of the five; (V) Inline with copyright row — 48 px badges sit on the same line as the legal text, the only option that doesn't add vertical footer height. All five reuse the two existing brand SVGs and require a one-line swap in `Footer.jsx` plus an optional `tone`/`size` prop on `RatingProof.jsx`. |
| `tests/sketches/trust-strip-concepts.html` | Six horizontal credibility-strip drafts to sit above `.ht-stats-area` in `src/pages/Home.jsx:133` (the 14 / 100 / 32 cream tile), split on the recognition / social-proof axis because the reference card-stack conflated them (an institutional award seal stacked on top of two consumer-rating widgets). Each draft renders the strip on `--ht-body` cream with a faithful copy of the existing stats tile underneath so the visual handshake is testable. **Axis A · International recognition** (press, awards, accreditations) — (A.I) Editorial press line — "AS SEEN IN" tag + five wordmarks (FT, Economist, Bloomberg, Robb Report, IMI Connect) between two ink hairlines, magazine-masthead register; (A.II) Accreditation seals — five gold coin discs (Corporate Immigration Awards 2019, IMC Member, CFA Charterholder, IBA Affiliate, FATF Aligned), reuses the wax-seal direction from the freshness-badge round two; (A.III) Pull-quote ribbon — single italic press quote in Newsreader 22 px, oversized gold quote mark, hairline-divided meta column with two factual anchors (CIA 2019 · IMC since 2018). **Axis B · Social proof** (Google / Trustpilot / Clutch / awards seal) — (B.I) Reference-fidelity three-card row — closest possible mirror of the original screenshot turned horizontal, with each card in its source's native brand language: Google G in 4-colour glyph + yellow top-rule, Trustpilot in green-square stars with green word logo, Corporate Immigration Awards in red double-ring seal; highest source-recognition value, loudest visually; (B.II) Editorial-reskin three-card row — same three datapoints reskinned to Mobius palette (cream cards, 2 px gold top-rules, italic Cormorant 44 px rating numerals echoing the 80 px stats numerals, gold stars, source word in small-caps Manrope); loses brand-recognition signal, gains visual coherence with the page; (B.III) Doc-control aggregator — monospace audit row reusing `.ht-breadcrumb__doc-control` DNA verbatim (three tabular cells: GOOGLE · TRUSTPILOT · CLUTCH with green-dot current indicators, italic Cormorant 36 px rating, monospace "n reviews · since YYYY" sub-line); lowest visual mass, fits the dossier voice. Institution names + rating numbers are placeholders pending verified Mobius source counts. |

## Viewing

```bash
# Quickest: any static server pointed at the repo root
npx serve .
# then open http://localhost:3000/tests/sketches/program-page-improvements.html
```

`vite dev` won't serve `/tests/` — that's intentional. Use a plain static
server when reviewing drafts.
