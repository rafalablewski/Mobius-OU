# /tests/ — drafts, sketches and design R&D

Scratchpad for things that **aren't** part of the deployed site. Anything in
this folder is for review, exploration or comparison and should not be linked
from the production app.

## Convention

- All draft sketches, wireframes and design R&D go under
  `/tests/sketches/<name>.html` (or `.svg`).
- Standalone exploratory pages (logo concepts, palette tests, type specimens,
  competitor comparisons) live at the top of `/tests/`.
- Nothing in `/tests/` ships in the Vite build — it sits outside `public/` and
  outside `src/` so it never reaches `dist/`.
- If a sketch becomes a real component, port it into `src/components/` and
  delete the draft.

## Current contents

| Path | What |
| --- | --- |
| `tests/logo-concepts.html` | Standalone preview of logo wordmark concepts (was `public/assets/logo-concepts.html`). |
| `tests/sketches/program-page-improvements.html` | Five proposed improvements to `ProgramPage.jsx` benchmarked against Astons' Vanuatu landing — ideas + hand-drawn-style wireframes. Includes a second section with five UX variations of idea 01's "Brief a Partner" panel (inline strip, email-gated dossier, expanding pill, dock-on-scroll, calendar-first). |
| `tests/sketches/freshness-badge-redesign.html` | Fifteen redesigns of the program-page "UPDATED 2026.Q1" badge (`Breadcrumb.jsx` `freshness` prop), in three rounds. Round one (pragmatic): dossier stamp, masthead line, changelog tab, gutter mark, status pill + tooltip. Round two (bolder, decorative): wax-seal monogram, watermark chapter mark, postage stamp, redline correction, almanac · roman numeral. Round three (institutional, restrained — audit-report register): document-control block, ISO datestamp, filing reference table, semver tag, standfirst compliance line. Each shown as a pencil sketch on dark hero ground plus a live HTML/CSS micro-mock. |
| `tests/sketches/calendar-redesign-concepts.html` | Ten redesigns of the program-page booking card (`BriefPanel.jsx` — "Pick a 30-minute slot"), drawing on Apple, Tesla, Lucid Motors and Adobe. Apple — Stocks-style frosted card with weekday chips; Apple Wallet perforated boarding-pass stub. Tesla — touchscreen slab with oversized 88px time and single CTA; Tesla service-appointment list with "soonest" tag and host monogram. Lucid — horizontal hairline cluster with three pips; Lucid Air interior with champagne monogram and three glass blades. Adobe — Creative Cloud panel with per-row swatches and ⌥1/⌥2/⌥3 keyboard shortcuts; Adobe XD split dock pairing month grid + day's slot list. Hybrids — Apple × Lucid clock-face dial picker; Tesla × Adobe split panel that exposes booked-vs-open scarcity. Each is a live HTML/CSS render on dark hero ground, with TOC, baseline of today's card, and a per-idea "vs. today" contrast note. |
| `tests/sketches/calendar-editorial-fit-ideas.html` | Ten visual variants for the shipped Tesla `BriefPanel` whose only goal is to make it fit the editorial page register without changing structure or behaviour — the panel currently reads as software dropped onto a dossier. Same data + same logic in every panel; only typography, colour, ornament and surface change. Each rendered desktop + mobile side-by-side; one shared timer drives every live countdown. (01) Cream card invert — full palette flip to cream BG / dark text / italic Fraunces bigtime / dark CTA slab — the boldest swap of the ten; (02) Doc-control register — booking panel becomes another doc-control row in the page's audit format (HOST · STATUS · OPEN · CLOSES tabular cells, hairline-bordered slot grid, ghost-outline CTA), reusing the existing `.ht-breadcrumb__doc-control` pattern verbatim; (03) Letterpress almanac — full editorial serif on a parchment cream ground, Cormorant italic 300 at 88 px for the bigtime, CTA becomes a serif italic line bracketed by em-dashes (no button); (04) Pitch-card frame — reuses the page's `.ht-pitch-card` surface verbatim (1 px gold border at 30 %, white-on-black gradient, top-left + bottom-right corner ticks), Fraunces italic numerals, hairline gold ghost CTA; (05) Magazine sidecar — print-magazine sidebar with thick masthead rule, italic byline, drop-cap-style bigtime, dotted-rule slot list; (06) Watermark monogram — 280 px Fraunces italic 'M' behind the content at 6 % opacity, otherwise dark slab with serif italic numerals; (07) Wax-seal accent — 56 px circular gold seal at the top-right corner with dashed inner ring and italic 'M' rotated 8°, otherwise restrained dark slab — reuses the freshness-badge round-two seal direction; (08) Calling card centred — formal cream calling-card layout with double gold rules near top + bottom edges, 96 px Cormorant italic centrepiece, dotted-rule slot list, hairline-bordered Reserve pill; (09) Linen letterhead — laid-paper texture (CSS-only cross-weave) on warm cream, letterhead masthead with Cormorant italic wordmark + monospace reference tag, gold-outline CTA; (10) Tesla-restrained editorial — most conservative: keeps the shipped layout exactly, only shifts Manrope-200 → Fraunces italic 300 for the numerals, white border → low-opacity gold, solid white CTA → cream-outline pill. |
| `tests/sketches/program-navigation-concepts.html` | Ten navigation patterns for threading a nine-chapter spine (About · Investment · Benefits · Eligibility · Documents · Process · Visa-Free Countries · Case Studies · Our Licenses) through `ProgramPage.jsx` between the hero and the dossier body, with each chapter occupying its own 100vh panel and one wheel-tick = one chapter (scroll-snap). Each idea: pencil sketch on cream + live HTML/CSS micro-mock on dark hero ground + behaviour register (scroll · active · keyboard · mobile) + "vs today" contrast. (I) Roman index card — front-matter "Contents" page-zero with I–IX entries before the chapters; (II) sticky pill rail — 56-px bar pinned beneath the hero with horizontal pill list, gold underline tracker animates between active labels; (III) left-edge chapter spine — vertical 64-px rail with rotated chapter labels and Roman numerals, gold dot marks active, hover-expands to full-width; (IV) right-edge dot navigator — Apple-product-page restraint, nine 4-px dots on right edge with hover-revealed flag, active dot fills gold; (V) numbered progress rail — top filmstrip with nine 01–09 ticks, gold rule fills left-to-right as reader progresses, drop-anchored serif title above active tick; (VI) file-folder paper tabs — skeuomorphic manila tabs fanned across folder top, active tab forward + gold-edged, siblings paper-grey one notch lower; (VII) big-numeral chapter parker — no menu, each chapter opens with 240-px italic Cormorant Roman numeral parked top-left + tiny "02 / 09" counter top-right that expands into a TOC overlay on click; (VIII) wax-seal mark + bottom dock — decorative wax-seal monogram with Roman numeral rotated 8° opens each chapter (reuses freshness-badge round-two), functional ‹ prev · here · next › slim dock pinned to bottom carries the controls; (IX) floating contents drawer — single corner pill "Contents · II/IX" with ⌘K shortcut opens centred Linear-style command-palette overlay listing all nine chapters, otherwise zero chrome; (X) doc-control register tabs — adds one more tabular row beneath the existing `.ht-breadcrumb__doc-control` pattern, nine monospace cells (I·ABOUT, II·INVT-02, …) with green-dot ON-PAGE indicator on the active cell, zero new design vocabulary. |
| `tests/sketches/calendar-fomo-countdown-ideas.html` | Fifteen polish variants for the shipped Tesla `BriefPanel`. Each combines: a FOMO scarcity signal ("2 left"), a live countdown chip, a specific mobile reflow, plus three constants — host **Rafał A.**, an **AVAILABLE** indicator with a green flashing dot in the top-right, and the classic **NDA-FIRST · NO MARKETING · PRIVATE CALL** footer microcopy alongside a serif "Pick another time →" alt link. Desktop + mobile rendered side-by-side per idea on dark hero ground; one shared JS timer drives every live counter. **Reworked v2 (01–10):** (01) gold "2 LEFT" pill in slim countdown bar; (02) editorial serif italic "Two slots left — Rafał's week closes in 2 h 14 m"; (03) 12-pip warm capacity rail + flip-clock HH/MM/SS + scroll-snap mobile; (04) feed strip carrying both social proof + "2 left" + warm foot timer + bottom-sheet mobile; (05) per-slab tags (LAST / 2 LEFT / OPEN) + denser 3-up; (06) terracotta stopwatch row "Closes today · 2 left · 02:14:38" + sticky-CTA mobile; (07) 35° corner ribbon "ALMOST FULL · 2 LEFT" + inline meta-line timer + drawer-up animation; (08) breathing chip "2 LEFT · closes HH:MM:SS"; (09) split-status pair row + per-slab chips + collapsed mini-bar mobile; (10) animated gold hairline rule "2 LEFT this week · 02:14:38". **New v2 (11–15):** (11) marquee status ticker scrolling AVAILABLE · 2 LEFT · CLOSES IN 02:14:38 · LAST RESERVED 4 MIN AGO with edge-mask gradients (22 s desktop / 28 s mobile); (12) circular SVG progress arc that depletes around the live HH:MM, with "2 LEFT" pill alongside; (13) audit-report doc-control register — monospace tabular row STATUS · OPEN · CLOSES with green-dot AVAILABLE inline (matches the program-page doc-control); (14) twin-counter pairing "Next slot in 18 h 23 m" (neutral) with "Window closes in 02:14:38 · 2 LEFT" (warm), cells stack on mobile; (15) reserved-stamp slabs — whole-week 5-up grid with redlined "BOOKED" stamps on three days so the two open slots pop visually, countdown in the legend row. |

## Viewing

```bash
# Quickest: any static server pointed at the repo root
npx serve .
# then open http://localhost:3000/tests/sketches/program-page-improvements.html
```

`vite dev` won't serve `/tests/` — that's intentional. Use a plain static
server when reviewing drafts.
