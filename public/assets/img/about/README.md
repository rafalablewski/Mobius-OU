# About imagery

Drop the principal portrait here as `principal.jpg` (4 : 5 aspect, 1200 × 1500
or larger, WebP / AVIF preferred). Referenced from `src/sections/About.jsx`.

If the file is missing the About section falls back to a navy gradient, so
staging without the final portrait still reads cleanly.
